{
  "name": "trip",
  "private": true,
  "scripts": {
    "routes:cache": "node shared/tools/precompute-routes.mjs",
    "precompute:all": "npm run routes:cache",
    "test:timeline": "node shared/tools/test-timeline-model.mjs"
  },
  "devDependencies": {
    "playwright": "^1.52.0"
  }
}
